<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scatch - Luxury Bags</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600&family=Inter:wght@300;400;500&display=swap" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #f8f6f2 0%, #f0ede6 100%);
            min-height: 100vh;
            font-family: 'Inter', sans-serif;
            margin: 0;
            padding: 0;
        }
        .navbar {
            background: linear-gradient(135deg, #8B4513 0%, #A0522D 100%);
            padding: 1rem 0;
        }
        .navbar-brand {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem;
            font-weight: 600;
            color: white !important;
        }
        .nav-link {
            color: rgba(255,255,255,0.9) !important;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        .nav-link:hover {
            color: white !important;
            transform: translateY(-1px);
        }
        .shop-header {
            background: white;
            padding: 3rem 0;
            border-bottom: 1px solid #e8dfd4;
        }
        .shop-title {
            font-family: 'Playfair Display', serif;
            color: #5D4037;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }
        .shop-subtitle {
            color: #8B4513;
            font-weight: 500;
        }
        .filter-section {
            background: white;
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(139, 69, 19, 0.08);
            margin-bottom: 2rem;
            border: 1px solid #f0e6dc;
        }
        .filter-title {
            font-family: 'Playfair Display', serif;
            color: #5D4037;
            font-weight: 600;
            margin-bottom: 1.5rem;
        }
        .filter-group {
            margin-bottom: 1.5rem;
        }
        .filter-label {
            font-weight: 600;
            color: #5D4037;
            margin-bottom: 0.5rem;
            display: block;
        }
        .form-select, .form-check-input {
            border: 1.5px solid #e8dfd4;
            border-radius: 8px;
            padding: 10px;
            background: #fcfaf7;
        }
        .form-select:focus, .form-check-input:focus {
            border-color: #8B4513;
            box-shadow: 0 0 0 3px rgba(139, 69, 19, 0.1);
        }
        .form-check-label {
            color: #5D4037;
            font-weight: 500;
        }
        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 2rem;
            margin-top: 2rem;
        }
        .product-card {
            background: white;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 4px 20px rgba(139, 69, 19, 0.08);
            transition: all 0.3s ease;
            border: 1px solid #f0e6dc;
        }
        .product-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 30px rgba(139, 69, 19, 0.15);
        }
        .product-image {
            width: 100%;
            height: 200px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-family: 'Playfair Display', serif;
            font-size: 1.2rem;
            overflow: hidden;
        }
        .product-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        .product-info {
            padding: 1.5rem;
        }
        .product-name {
            font-family: 'Playfair Display', serif;
            color: #5D4037;
            font-weight: 600;
            font-size: 1.2rem;
            margin-bottom: 0.5rem;
        }
        .product-price {
            color: #8B4513;
            font-weight: 600;
            font-size: 1.3rem;
            margin-bottom: 1rem;
        }
        .product-original-price {
            color: #999;
            text-decoration: line-through;
            font-size: 1rem;
            margin-left: 0.5rem;
        }
        .product-discount {
            background: #8B4513;
            color: white;
            padding: 0.3rem 0.8rem;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
            display: inline-block;
            margin-bottom: 1rem;
        }
        .btn-primary {
            background: linear-gradient(135deg, #8B4513 0%, #A0522D 100%);
            border: none;
            padding: 12px 24px;
            font-weight: 500;
            border-radius: 8px;
            transition: all 0.3s ease;
            width: 100%;
        }
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(139, 69, 19, 0.3);
        }
        .sort-section {
            background: white;
            padding: 1rem 1.5rem;
            border-radius: 8px;
            margin-bottom: 2rem;
            border: 1px solid #e8dfd4;
        }
        .section-divider {
            height: 2px;
            background: linear-gradient(90deg, transparent, #8B4513, transparent);
            margin: 2rem 0;
        }
        .no-products {
            text-align: center;
            padding: 3rem;
            color: #5D4037;
        }
        .no-products h3 {
            font-family: 'Playfair Display', serif;
            margin-bottom: 1rem;
        }
    </style>
</head>
<body>
    <!-- Flash Messages -->
    <% if (success_msg && success_msg.length > 0) { %>
        <div class="alert alert-success alert-dismissible fade show" role="alert" style="margin: 0; border-radius: 0;">
            <%= success_msg %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    <% } %>

    <% if (error_msg && error_msg.length > 0) { %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert" style="margin: 0; border-radius: 0;">
            <%= error_msg %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    <% } %>

    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="/">Scatch</a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/">Home</a>
                <a class="nav-link active" href="/shop">Shop</a>
                <a class="nav-link" href="/users/cart">Cart</a>
                <a class="nav-link" href="#">Contact</a>
                <a class="nav-link" href="/users/logout">Logout</a>
            </div>
        </div>
    </nav>

    <!-- Shop Header -->
    <div class="shop-header">
        <div class="container text-center">
            <h1 class="shop-title">Luxury Bag Collection</h1>
            <p class="shop-subtitle">Discover our exquisite range of premium handcrafted bags</p>
        </div>
    </div>

    <div class="container">
        <div class="row">
            <!-- Filters Sidebar -->
            <div class="col-md-3">
                <div class="filter-section">
                    <h4 class="filter-title">Filter by:</h4>
                    
                    <div class="filter-group">
                        <label class="filter-label">Availability</label>
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" id="inStock">
                            <label class="form-check-label" for="inStock">In Stock</label>
                        </div>
                    </div>

                    <div class="filter-group">
                        <label class="filter-label">Discount</label>
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" id="discounted">
                            <label class="form-check-label" for="discounted">Discounted Items</label>
                        </div>
                    </div>

                    <div class="filter-group">
                        <label class="filter-label">Collections</label>
                        <select class="form-select">
                            <option selected>All Collections</option>
                            <option>New Collection</option>
                            <option>Classic Series</option>
                            <option>Limited Edition</option>
                        </select>
                    </div>
                </div>
            </div>

            <!-- Products Grid -->
            <div class="col-md-9">
                <!-- Sort Section -->
                <div class="sort-section d-flex justify-content-between align-items-center">
                    <div>
                        <strong>Sort by:</strong>
                        <select class="form-select d-inline-block w-auto ms-2">
                            <option selected>Popular</option>
                            <option>Newest</option>
                            <option>Price: Low to High</option>
                            <option>Price: High to Low</option>
                        </select>
                    </div>
                    <div class="btn-group">
                        <button class="btn btn-outline-secondary active">All Products</button>
                        <button class="btn btn-outline-secondary">New Collection</button>
                        <button class="btn btn-outline-secondary">Discounted Products</button>
                    </div>
                </div>

                <div class="section-divider"></div>

                <!-- Products Grid -->
                <div class="products-grid">
                    <% if (products && products.length > 0) { %>
                        <% products.forEach(function(product) { %>
                            <div class="product-card">
                                <div class="product-image" style="background: <%- product.bgcolor || '#8B4513' %>;">
                                    <% if (product.image) { %>
                                        <img src="data:<%- product.imageType %>;base64,<%- product.image.toString('base64') %>" alt="<%- product.name %>">
                                    <% } else { %>
                                        <span style="color: white; font-size: 0.9rem; padding: 1rem; text-align: center;"><%- product.name %></span>
                                    <% } %>
                                </div>
                                <div class="product-info">
                                    <h3 class="product-name"><%- product.name %></h3>
                                    <div class="d-flex align-items-center">
                                        <span class="product-price">$<%- product.price %></span>
                                        <% if (product.discount && product.discount > 0) { %>
                                            <% 
                                                let originalPrice = product.price / (1 - product.discount / 100);
                                                let discountAmount = originalPrice - product.price;
                                            %>
                                            <span class="product-original-price">$<%- originalPrice.toFixed(2) %></span>
                                        <% } %>
                                    </div>
                                    <% if (product.discount && product.discount > 0) { %>
                                        <span class="product-discount"><%- product.discount %>% OFF</span>
                                    <% } %>
                                    <form action="/users/cart/add" method="POST" style="display: inline;">
                                        <input type="hidden" name="productId" value="<%= product._id %>">
                                        <button type="submit" class="btn btn-primary">Add to Cart</button>
                                    </form>
                                </div>
                            </div>
                        <% }); %>
                    <% } else { %>
                        <div class="col-12">
                            <div class="no-products">
                                <h3>No Products Available</h3>
                                <p>Check back later for new arrivals!</p>
                                <a href="/products/create" class="btn btn-primary">Add New Product</a>
                            </div>
                        </div>
                    <% } %>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Simple filter functionality
        document.addEventListener('DOMContentLoaded', function() {
            const filterCheckboxes = document.querySelectorAll('.form-check-input');
            
            filterCheckboxes.forEach(checkbox => {
                checkbox.addEventListener('change', function() {
                    // Add your filter logic here
                    console.log('Filter changed:', this.id, this.checked);
                });
            });

            // Sort functionality
            const sortSelect = document.querySelector('.form-select');
            sortSelect.addEventListener('change', function() {
                console.log('Sort by:', this.value);
            });

            // Auto-dismiss flash messages after 5 seconds
            const alerts = document.querySelectorAll('.alert');
            alerts.forEach(alert => {
                setTimeout(() => {
                    const bsAlert = new bootstrap.Alert(alert);
                    bsAlert.close();
                }, 5000);
            });
        });
    </script>
</body>
</html>