<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create New Product</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background-color: #f5f7fa;
            color: #333;
            line-height: 1.6;
            padding: 20px;
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
            overflow: hidden;
        }
        
        header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            text-align: center;
        }
        
        h1 {
            font-size: 2.5rem;
            margin-bottom: 10px;
            font-weight: 300;
        }
        
        .subtitle {
            font-size: 1.1rem;
            opacity: 0.9;
        }
        
        .form-section {
            padding: 30px;
        }
        
        .section-title {
            font-size: 1.4rem;
            color: #4a5568;
            margin-bottom: 25px;
            padding-bottom: 10px;
            border-bottom: 2px solid #e2e8f0;
            font-weight: 500;
        }
        
        .form-group {
            margin-bottom: 25px;
        }
        
        label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #2d3748;
            font-size: 0.95rem;
        }
        
        input[type="text"],
        input[type="number"],
        input[type="file"] {
            width: 100%;
            padding: 12px 15px;
            border: 2px solid #e2e8f0;
            border-radius: 8px;
            font-size: 16px;
            transition: all 0.3s ease;
            background: white;
        }
        
        input[type="text"]:focus,
        input[type="number"]:focus {
            border-color: #667eea;
            outline: none;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }
        
        .file-upload-wrapper {
            position: relative;
        }
        
        .file-upload-label {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 12px 15px;
            border: 2px dashed #cbd5e0;
            border-radius: 8px;
            background: #f7fafc;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .file-upload-label:hover {
            border-color: #667eea;
            background: #edf2f7;
        }
        
        .file-name {
            color: #718096;
            font-size: 14px;
        }
        
        .file-input {
            position: absolute;
            left: 0;
            top: 0;
            opacity: 0;
            width: 100%;
            height: 100%;
            cursor: pointer;
        }
        
        .color-section {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-top: 10px;
        }
        
        .color-group {
            background: #f7fafc;
            padding: 20px;
            border-radius: 8px;
            border: 1px solid #e2e8f0;
        }
        
        .color-group label {
            margin-bottom: 15px;
        }
        
        .color-picker {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
        }
        
        .color-option {
            display: flex;
            flex-direction: column;
            align-items: center;
            cursor: pointer;
        }
        
        .color-box {
            width: 40px;
            height: 40px;
            border-radius: 6px;
            margin-bottom: 5px;
            border: 3px solid transparent;
            transition: all 0.3s ease;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        
        .color-box:hover {
            transform: scale(1.1);
        }
        
        .color-box.selected {
            border-color: #2d3748;
            transform: scale(1.1);
        }
        
        .color-label {
            font-size: 11px;
            color: #718096;
            font-weight: 500;
        }
        
        .submit-section {
            text-align: center;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #e2e8f0;
        }
        
        .submit-btn {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            padding: 15px 40px;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
        }
        
        .submit-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.4);
        }
        
        .submit-btn:active {
            transform: translateY(0);
        }
        
        .preview-section {
            margin-top: 30px;
            padding: 25px;
            background: #f7fafc;
            border-radius: 8px;
            border: 1px solid #e2e8f0;
        }
        
        .preview-title {
            font-size: 1.2rem;
            color: #4a5568;
            margin-bottom: 15px;
            font-weight: 600;
        }
        
        .preview-content {
            display: grid;
            grid-template-columns: 1fr 2fr;
            gap: 20px;
            align-items: start;
        }
        
        .preview-image {
            background: white;
            border-radius: 8px;
            height: 150px;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 2px dashed #cbd5e0;
            overflow: hidden;
        }
        
        .preview-image img {
            max-width: 100%;
            max-height: 100%;
            display: none;
        }
        
        .preview-details {
            background: white;
            padding: 20px;
            border-radius: 8px;
            border: 1px solid #e2e8f0;
        }
        
        .preview-product-name {
            font-size: 1.3rem;
            font-weight: 600;
            color: #2d3748;
            margin-bottom: 10px;
        }
        
        .preview-price {
            font-size: 1.5rem;
            color: #e53e3e;
            font-weight: 700;
            margin-bottom: 15px;
        }
        
        .preview-panels {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
            margin-top: 15px;
        }
        
        .preview-panel {
            padding: 15px;
            border-radius: 6px;
            text-align: center;
            color: white;
            font-weight: 600;
            font-size: 0.9rem;
        }
        
        .message {
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            text-align: center;
            font-weight: 500;
        }
        
        .message.success {
            background: #c6f6d5;
            color: #22543d;
            border: 1px solid #9ae6b4;
        }
        
        .message.error {
            background: #fed7d7;
            color: #742a2a;
            border: 1px solid #feb2b2;
        }
        
        @media (max-width: 768px) {
            .color-section {
                grid-template-columns: 1fr;
            }
            
            .preview-content {
                grid-template-columns: 1fr;
            }
            
            .preview-panels {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Scratch</h1>
            <div class="subtitle">Create New Product</div>
        </header>
        
        <div class="form-section">
            <!-- Display success/error messages -->
            <% if (typeof message !== 'undefined' && message) { %>
                <div class="message <%= message.type %>">
                    <%= message.text %>
                </div>
            <% } %>
            
            <!-- FIXED FORM TAG - removed the extra closing tag -->
            <form action="/products/create" method="post" enctype="multipart/form-data" id="productForm">
                <h2 class="section-title">Product Details</h2>
                
                <div class="form-group">
                    <label for="productImage">Product Image</label>
                    <div class="file-upload-wrapper">
                        <label class="file-upload-label">
                            <span>Choose File</span>
                            <span class="file-name" id="fileName">No file chosen</span>
                        </label>
                        <input type="file" name="image" id="image" class="file-input" accept="image/*" required>
                    </div>
                </div>
                
                <div class="form-group">
                    <label for="productName">Product Name</label>
                    <input type="text" name="name" id="productName" placeholder="Enter product name" required>
                </div>
                
                <div class="form-group">
                    <label for="discountPrice">Price ($)</label>
                    <input type="number" name="price" id="discountPrice" placeholder="0.00" step="0.01" min="0" required>
                </div>
                
                <h2 class="section-title">Panel Details</h2>
                
                <div class="color-section">
                    <div class="color-group">
                        <label>Background Color</label>
                        <div class="color-picker">
                            <div class="color-option">
                                <div class="color-box selected" style="background-color: #3498db;" data-color="#3498db"></div>
                                <span class="color-label">Blue</span>
                            </div>
                            <div class="color-option">
                                <div class="color-box" style="background-color: #2ecc71;" data-color="#2ecc71"></div>
                                <span class="color-label">Green</span>
                            </div>
                            <div class="color-option">
                                <div class="color-box" style="background-color: #e74c3c;" data-color="#e74c3c"></div>
                                <span class="color-label">Red</span>
                            </div>
                            <div class="color-option">
                                <div class="color-box" style="background-color: #f39c12;" data-color="#f39c12"></div>
                                <span class="color-label">Orange</span>
                            </div>
                            <div class="color-option">
                                <div class="color-box" style="background-color: #9b59b6;" data-color="#9b59b6"></div>
                                <span class="color-label">Purple</span>
                            </div>
                            <div class="color-option">
                                <div class="color-box" style="background-color: #34495e;" data-color="#34495e"></div>
                                <span class="color-label">Dark</span>
                            </div>
                        </div>
                        <input type="hidden" name="bgcolor" id="backgroundColor" value="#3498db">
                    </div>
                    
                    <div class="color-group">
                        <label>Tool Color</label>
                        <div class="color-picker">
                            <div class="color-option">
                                <div class="color-box selected" style="background-color: #2c3e50;" data-color="#2c3e50"></div>
                                <span class="color-label">Navy</span>
                            </div>
                            <div class="color-option">
                                <div class="color-box" style="background-color: #7f8c8d;" data-color="#7f8c8d"></div>
                                <span class="color-label">Gray</span>
                            </div>
                            <div class="color-option">
                                <div class="color-box" style="background-color: #ecf0f1;" data-color="#ecf0f1"></div>
                                <span class="color-label">Light</span>
                            </div>
                            <div class="color-option">
                                <div class="color-box" style="background-color: #e74c3c;" data-color="#e74c3c"></div>
                                <span class="color-label">Red</span>
                            </div>
                            <div class="color-option">
                                <div class="color-box" style="background-color: #f39c12;" data-color="#f39c12"></div>
                                <span class="color-label">Orange</span>
                            </div>
                            <div class="color-option">
                                <div class="color-box" style="background-color: #27ae60;" data-color="#27ae60"></div>
                                <span class="color-label">Green</span>
                            </div>
                        </div>
                        <input type="hidden" name="panalcolor" id="toolColor" value="#2c3e50">
                    </div>
                </div>
                
                <!-- Preview Section -->
                <div class="preview-section">
                    <div class="preview-title">Product Preview</div>
                    <div class="preview-content">
                        <div class="preview-image">
                            <img id="imagePreview" src="#" alt="Product Preview">
                            <span id="noImageText">No image selected</span>
                        </div>
                        <div class="preview-details">
                            <div class="preview-product-name" id="previewProductName">Product Name</div>
                            <div class="preview-price" id="previewPrice">$0.00</div>
                            <div class="preview-panels">
                                <div class="preview-panel" id="previewBackgroundPanel" style="background-color: #3498db;">
                                    Background
                                </div>
                                <div class="preview-panel" id="previewToolPanel" style="background-color: #2c3e50;">
                                    Tool Panel
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="submit-section">
                    <button type="submit" class="submit-btn">Create New Product</button>
                </div>
            </form> <!-- This is the correct closing form tag -->
        </div>
    </div>

    <script>
        // File upload preview
        document.getElementById('image').addEventListener('change', function(e) {
            const file = e.target.files[0];
            const fileName = file ? file.name : 'No file chosen';
            document.getElementById('fileName').textContent = fileName;
            
            // Show image preview
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    document.getElementById('imagePreview').src = e.target.result;
                    document.getElementById('imagePreview').style.display = 'block';
                    document.getElementById('noImageText').style.display = 'none';
                }
                reader.readAsDataURL(file);
            }
        });

        // Color selection
        document.querySelectorAll('.color-box').forEach(box => {
            box.addEventListener('click', function() {
                const parentGroup = this.closest('.color-group');
                
                // Remove selected class from all boxes in this group
                parentGroup.querySelectorAll('.color-box').forEach(b => {
                    b.classList.remove('selected');
                });
                
                // Add selected class to clicked box
                this.classList.add('selected');
                
                // Update hidden input value
                const colorValue = this.getAttribute('data-color');
                const hiddenInput = parentGroup.querySelector('input[type="hidden"]');
                hiddenInput.value = colorValue;
                
                // Update preview
                if (hiddenInput.id === 'backgroundColor') {
                    document.getElementById('previewBackgroundPanel').style.backgroundColor = colorValue;
                } else if (hiddenInput.id === 'toolColor') {
                    document.getElementById('previewToolPanel').style.backgroundColor = colorValue;
                }
            });
        });
        
        // Real-time preview updates
        document.getElementById('productName').addEventListener('input', function() {
            document.getElementById('previewProductName').textContent = this.value || 'Product Name';
        });
        
        document.getElementById('discountPrice').addEventListener('input', function() {
            const price = this.value ? parseFloat(this.value).toFixed(2) : '0.00';
            document.getElementById('previewPrice').textContent = '$' + price;
        });
        
        // Form validation
        document.getElementById('productForm').addEventListener('submit', function(e) {
            const productName = document.getElementById('productName').value.trim();
            const discountPrice = document.getElementById('discountPrice').value;
            const productImage = document.getElementById('image').files[0];
            
            if (!productName) {
                e.preventDefault();
                alert('Please enter a product name');
                return;
            }
            
            if (!discountPrice || parseFloat(discountPrice) <= 0) {
                e.preventDefault();
                alert('Please enter a valid discount price');
                return;
            }
            
            if (!productImage) {
                e.preventDefault();
                alert('Please select a product image');
                return;
            }
        });
    </script>
</body>
</html>